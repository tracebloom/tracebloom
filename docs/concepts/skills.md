# Skills

A skill is a reusable capability the agent invokes — a tool, a sub-agent, a retrieval pipeline. tracebloom shows skill usage as a timeline so you can see which skills dominate a session and which rarely fire.
