# Memory

Memory is any persistent state the agent reads from or writes to across turns: scratchpads, vector stores, key-value caches, summaries. tracebloom records each access as a span attached to the enclosing decision.
